import discord
from discord.ext import commands
from utils.helpers import format_currency


class HelpCog(commands.Cog):
    """Справка по командам"""
    
    def __init__(self, bot):
        self.bot = bot
    
    @commands.command(name="помощь", aliases=["help", "команды"])
    async def help_command(self, ctx):
        """Показать справку по командам"""
        embed = discord.Embed(
            title="⚓ Элаим - Справка",
            description="Бот для управления флотом в Highfleet",
            color=0x3498db
        )
        
        embed.add_field(
            name="📝 Регистрация",
            value="`!анкета` - Создать флотилию\n"
                  "`!флот` - Показать ваш флот\n"
                  "`!флот @игрок` - Посмотреть чужой флот",
            inline=False
        )
        
        embed.add_field(
            name="🚀 Управление флотом",
            value="`!добавить_корабль` - Добавить корабль (интерактивно)\n"
                  "`!удалить_корабль [позывной]` - Удалить корабль\n"
                  "`!обновить_экипаж [позывной] [число]` - Изменить экипаж\n"
                  "`!статус [позывной] [статус]` - Изменить статус повреждений",
            inline=False
        )
        
        embed.add_field(
            name="📊 Калькуляторы",
            value="`!расчет [тип] [экипаж] [км]` - Расчет ресурсов корабля\n"
                  "`!флот_расчет` - Расчет всего флота\n"
                  "`!метан [тип] [км]` - Расчет топлива",
            inline=False
        )
        
        embed.add_field(
            name="👑 Администратор",
            value="`!ход` - Следующий игровой ход\n"
                  "`!дать_ресурсы @игрок [тип] [количество]` - Выдать ресурсы\n"
                  "`!сбросить @игрок` - Удалить флот\n"
                  "`!перелет @игрок [км] [название] [спец]` - Переместить флот",
            inline=False
        )

        embed.add_field(
            name="🏪 Рынок и Инвентарь",
            value="`!склад` - Показать инвентарь\n"
                  "`!магазин` - Товары в текущей локации\n"
                  "`!купить [ID] [кол-во]` - Купить модуль\n"
                  "`!продать [ID] [кол-во]` - Продать модуль\n"
                  "`!оснастить [позывной] [ID]` - Установить модуль\n"
                  "`!снять [позывной] [ID]` - Снять модуль",
            inline=False
        )

        embed.add_field(
            name="⚔️ Боевая система",
            value="`!бой @враг [дистанция]` - Начать сражение\n"
                  "В бою учитываются модули кораблей и их состояние.",
            inline=False
        )
        
        await ctx.send(embed=embed)


async def setup(bot):
    await bot.add_cog(HelpCog(bot))
